description "redis server"

start on runlevel [23]
stop on shutdown

exec su -s /bin/sh -c 'exec "$0" "$@"'  {{ redis_user }} -- {{ redis_prefix }}/bin/redis-server {{ redis_server_conf_path }}

respawn
